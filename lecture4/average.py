
"""
    mean function
"""

import statistics

print(statistics.mean([100, 90]))

"""
    # command-line arguments: allow me to provide input, not when prompted inside of a program 
        as happens whenever we call the python funcion input but rather, there is this feature 
        command-line arguments of programs that allows you to provide arguments that is input 
        to the program of just when you are executing at the command line.
"""